services:
  - type: web
    name: femis-web
    runtime: python
    buildCommand: cd femis-web && pip install -r requirements.txt
    startCommand: cd femis-web && gunicorn wsgi:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.11.0
